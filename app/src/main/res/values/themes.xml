<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CZ" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/black</item>
    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/input</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/white</item>
        <item name="buttonStyle">@style/ButtonStyle.NoColor</item><!-- Apply custom button style -->
        <!-- Add more customizations if needed -->
    </style>

    <style name="Theme.CZ" parent="AppTheme" />
    <style name="ButtonStyle.NoColor" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@android:color/transparent</item>
    </style>
</resources>
